# Claim 6 route 1 limitations

- Routes 1-4 are exact release, checkpoint, generator, and falsification audits, not faithful
  five-model training.
- The endpoint shortcut shows that the release does not isolate graph
  reachability complexity; it does not prove which feature a neural model used.
- The checkpoint architecture mismatch prevents using the sole released weight
  file as nonlinear-RNN evidence.
- The sole checkpoint can support only one architecture row and is not
  identified as a Figure 2 checkpoint.
- The fourth route did not find a valid assumption-satisfying counterexample.
  Claim 6 remains `BLOCKED` until the Figure checkpoints, seeds, logs,
  environment, and one consistent five-model protocol are available.
