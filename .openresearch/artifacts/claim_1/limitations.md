# Claim 1 limitations and deviations

- The paper's closed-form-to-FO-uniform-TC0 lemma and standard circuit closure
  facts are treated as trusted foundations.
- The certificate corrects the source's vector/matrix typing mismatch by using
  the dimensionally valid affine-vector recurrence.
- The numerical independent checker does not establish universality; only the
  symbolic composition and inductive resource argument do.
- This verifier addresses Claim 1 only. It does not establish the separate
  Boolean simulation depth in Claim 2.
